import React from 'react';

const Book = () => {
    return(
        <React.Fragment>
            <div className="right-col readerpanel">Reader Panel</div>
        </React.Fragment>
    );
}

export default Book;